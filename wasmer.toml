# wasmer.toml
name = "gorwanda-plus"
version = "1.0.0"

[app]
php_version = "8.3"

[server]
port = 8080

[build]
command = "composer install --no-dev --optimize-autoloader"

[env]
DATABASE_URL = "mysql://root:${MYSQL_PASSWORD}@${MYSQL_HOST}:3306/gorwanda_plus"